#include "videodirnavigator.h"

#include "infra/meteorcapturestate.h"

#include <QFileSystemModel>

VideoDirNavigator::VideoDirNavigator(MeteorCaptureState *state) : state(state)
{
//    QFileSystemModel *model = new QFileSystemModel;
//    model->setRootPath(QString::fromUtf8(state->videoDirPath.c_str()));
//    QTreeView *tree = new QTreeView(splitter);
//    tree->setModel(model);
}
