# Compiled source #
###################
*.com
*.class
*.dll
*.exe
*.o
*.so

# Packages #
############
# it's better to unpack these files and commit the raw source
# git has its own built in compression methods
*.7z
*.dmg
*.gz
*.iso
*.jar
*.rar
*.tar
*.zip

# Don't store any built version of the project (for now - might want to do this in future)
/build-*

# Don't store the local configuration; this will be regenerated on each build machine
MeteorCaptureQt/MeteorCaptureQt.pro.user

# Ignore backups created by text editore etc
*~
